export class UsageInfo {
    screenName: string;
    feature: string;
    usageCount: Number;

    // constructor(screen, count) {
    //     this.screen = screen;
    //     this.count = count;
    // }
}